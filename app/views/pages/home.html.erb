<div class="container-homepage">

  <!-- NFT Profile Picture -->
  <%= image_tag "rector_profile_image.png", alt: "RECTOR", class: "profile-picture" %>

  <!-- Introduction / Letter -->
  <div class="space-y-6">
    <p>
      Assalamu'alaikum! I'm RECTOR.
    </p>

    <p>
      Full-stack builder shipping products across web, blockchain, and beyond.
      I chase hackathons, contribute to open source, and share beneficial knowledge.
      Muslim developer working on projects that matter for dunya and akhirah.
    </p>

    <p>
      <strong>Vision:</strong> Build technology that serves humanity and pleases Allah SWT.<br>
      <strong>Mission:</strong> Ship excellent products, mentor communities, and preserve knowledge.<br>
      <strong>Passion:</strong> Decentralized systems, Islamic education tech, developer tools.
    </p>

    <p>
      Right now I'm working on building this space and
      <%= link_to "RZMetaversity", "https://github.com/rz1989s/RZMetaversity", target: "_blank", rel: "noopener" %> —
      a decentralized learning platform for Web3 builders.
      You can explore my <%= link_to "portfolio", "#" %>,
      peek at <%= link_to "labs", "#" %> experiments,
      read my <%= link_to "journal", "#" %>,
      or browse my <%= link_to "cheatsheet", "#" %>.
    </p>
  </div>

  <!-- Section Divider -->
  <hr class="section-divider" />

  <!-- Building in Public Section -->
  <section>
    <h2>Building in Public</h2>

    <% if @syncing %>
      <p class="mt-4">Loading latest projects from GitHub...</p>
    <% elsif @latest_projects.any? %>
      <div class="mt-4 space-y-6">
        <% @latest_projects.each do |project| %>
          <div>
            <p class="mb-2">
              <strong><%= link_to project.name, project.html_url, target: "_blank", rel: "noopener" %></strong>
              <% if project.language.present? %>
                <span class="text-xs"> • <%= project.language %></span>
              <% end %>
              <span class="text-xs"> • Updated <%= project.time_ago %></span>
            </p>
            <% if project.description.present? %>
              <p><%= sanitize project.description, tags: [] %></p>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="mt-4">No projects found. Check back later!</p>
    <% end %>

    <% if @tech_stack && @tech_stack[:primary].present? %>
      <p class="mt-6">
        Working across <%= @tech_stack[:total_repos] %> repositories.
        Primary stack:
        <%= @tech_stack[:primary].map { |l| l[:name] }.join(", ") %>.
      </p>
    <% end %>
  </section>

  <!-- Divider -->
  <hr class="section-divider" />

  <!-- Writing Section -->
  <section>
    <h2>Writing</h2>
    <p class="mt-4 mb-2">
      <strong>Latest article:</strong>
      <%= link_to "Coming Soon", "#" %>
    </p>
    <p>
      Journal section powered by Ghost CMS, integrated via API.
      Articles about code, faith, and building for eternity.
    </p>
  </section>

  <!-- Divider -->
  <hr class="section-divider" />

  <!-- Get in Touch -->
  <section>
    <h2>Get in Touch</h2>
    <p class="mt-4">
      <%= link_to "GitHub", "https://github.com/rz1989s", target: "_blank", rel: "noopener" %> •
      <%= link_to "X", "https://x.com/RZ1989sol", target: "_blank", rel: "noopener" %> •
      <%= link_to "Email", "mailto:rheza10@gmail.com" %>
    </p>
  </section>

</div>
