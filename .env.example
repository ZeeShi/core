# GitHub API Token
# Get your token at: https://github.com/settings/tokens
# Required scope: public_repo (read-only access to public repositories)
# Benefits: Increases rate limit from 60/hour to 5,000/hour
GITHUB_TOKEN=your_github_token_here

# Production Database (required in production only)
# Set this on your production server for database authentication
CORE_DATABASE_PASSWORD=your_secure_database_password_here

# Rails Secret Key Base (required in production only)
# Generate with: bin/rails secret
# Keep this secret and never commit it to version control
SECRET_KEY_BASE=your_secret_key_base_here
